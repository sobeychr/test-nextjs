import Layout from 'Global/Layout';

import DifficultyList from 'Items/DifficultyList';

const Items = () => (
    <Layout title='Items'>
        <DifficultyList />
    </Layout>
);

export default Items;
