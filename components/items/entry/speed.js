import { getItemById } from 'Data/items';

const ItemSpeed = ({id}) => {
    return (
        <div>
            <p className='subname'>speed</p>

            <style jsx>{`
            `}</style>
        </div>
    );
};

export default ItemSpeed;
